
const initialState = {
    showModal: false,
    blocks: [],
    isPosting: false
}

export const postsReducer = (state = initialState, action) => {
    switch(action.type) {
        case "SUBMIT_POST"
    }

}